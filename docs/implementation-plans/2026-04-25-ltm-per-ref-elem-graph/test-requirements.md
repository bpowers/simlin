# Test Requirements

Mapping each acceptance criterion in `2026-04-25-ltm-per-ref-elem-graph` to its automated test or human verification approach.

## Automated Tests

| AC | Test type | Test name | File path | Verifies |
|----|-----------|-----------|-----------|----------|
| ltm-per-ref-elem-graph.AC1.1 | unit | `element_graph_fixed_index_broadcast` (Phase 1 Task 1, fixed-index broadcast pattern) | /home/bpowers/src/simlin/src/simlin-engine/src/db_element_graph_tests.rs | `relative_pop[R] = population / population[NYC]` emits exactly 2N-1 unique edges (N diagonals plus N-1 broadcast-from-NYC), not N^2 |
| ltm-per-ref-elem-graph.AC1.1 | unit | `model_element_causal_edges` integration via Phase 2 Task 4 pivot | /home/bpowers/src/simlin/src/simlin-engine/src/db_analysis.rs | Pivot of `model_element_causal_edges` to AST-walking per-reference emission turns the Phase 1 red test green |
| ltm-per-ref-elem-graph.AC1.2 | unit | `element_graph_wildcard_reducer_plus_bare` (Phase 1 Task 1, wildcard reducer pattern) | /home/bpowers/src/simlin/src/simlin-engine/src/db_element_graph_tests.rs | `share[R] = population / SUM(population[*])` emits all 9 (N^2) edges including diagonal SameElement edges from the bare numerator |
| ltm-per-ref-elem-graph.AC1.3 | integration | `test_cross_element_ltm_edge_set_truthful` | /home/bpowers/src/simlin/src/simlin-engine/tests/simulate_ltm.rs | `test/cross_element_ltm/cross_element.stmx` produces truthful broadcast edges and no spurious all-pairs edges |
| ltm-per-ref-elem-graph.AC1.4 | proptest | Property test (32 cases) of variable-level projection invariant | /home/bpowers/src/simlin/src/simlin-engine/src/db_element_graph_proptest.rs | For randomly generated arrayed models, the variable-level projection of element edges equals `model_causal_edges` |
| ltm-per-ref-elem-graph.AC1.5 | unit | `element_graph_multidim_partial_fixed_conservative` (Phase 1 Task 1, multidim partial-fixed pattern) | /home/bpowers/src/simlin/src/simlin-engine/src/db_element_graph_tests.rs | Multidimensional sources with mixed literal+wildcard indices treat as Wildcard (conservative over-approximation) |
| ltm-per-ref-elem-graph.AC2.1 | unit | `test_partial_equation_share_bare_shape` | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_augment.rs | Bare-shape partial for `share = pop / SUM(pop[*])` leaves bare `pop` live and wraps wildcard reducer in PREVIOUS |
| ltm-per-ref-elem-graph.AC2.2 | unit | `test_partial_equation_share_wildcard_shape` | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_augment.rs | Wildcard-shape partial for `share = pop / SUM(pop[*])` wraps bare `pop` in PREVIOUS and leaves wildcard reducer live |
| ltm-per-ref-elem-graph.AC2.3 | unit | `test_partial_equation_migration_pressure_fixed_nyc` | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_augment.rs | FixedIndex(NYC) partial for `(pop[NYC] - pop[Boston]) * 0.01` yields `(pop[NYC] - PREVIOUS(pop[Boston])) * 0.01` |
| ltm-per-ref-elem-graph.AC2.3 | unit | `test_partial_equation_migration_pressure_fixed_boston` | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_augment.rs | FixedIndex(Boston) partial yields `(PREVIOUS(pop[NYC]) - pop[Boston]) * 0.01` |
| ltm-per-ref-elem-graph.AC2.4 | unit | `partial_equation_other_source_always_wrapped` | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_augment.rs | References to non-`live_source` deps are wrapped in PREVIOUS regardless of shape |
| ltm-per-ref-elem-graph.AC2.4 | unit | `partial_equation_unknown_ident_unchanged` | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_augment.rs | Unknown identifiers (not in deps) are left unchanged, not wrapped |
| ltm-per-ref-elem-graph.AC3.1 | integration | `per_shape_link_scores_for_share_with_sum` | /home/bpowers/src/simlin/src/simlin-engine/src/db_ltm_unified_tests.rs | `share[R] = pop / SUM(pop[*])` emits both Bare and Wildcard `LtmSyntheticVar`s for the (pop, share) pair |
| ltm-per-ref-elem-graph.AC3.1 | integration | `fixed_index_link_score_emits_per_element_name` | /home/bpowers/src/simlin/src/simlin-engine/src/db_ltm_unified_tests.rs | `rel_pop[R] = pop / pop[NYC]` emits Bare and FixedIndex(NYC) link score variables |
| ltm-per-ref-elem-graph.AC3.2 | unit | `link_score_name_fixed_index` | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_augment.rs | FixedIndex naming convention `$:ltm:link_score:{from}[{elem}]->{to}` is stable for FixedIndex shapes |
| ltm-per-ref-elem-graph.AC3.2 | unit | `parse_link_offsets` tests for FixedIndex A2A and FixedIndex scalar names | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_finding.rs | Discovery parser handles subscripted-from A2A and scalar names via existing `contains('[')` branch |
| ltm-per-ref-elem-graph.AC3.3 | unit | `link_score_name_bare_canonical` | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_augment.rs | Bare shape uses canonical `$:ltm:link_score:{from}->{to}` name |
| ltm-per-ref-elem-graph.AC3.3 | unit | `link_score_name_wildcard_always_suffixed` | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_augment.rs | Wildcard-shape link scores always carry `:wildcard` suffix regardless of model |
| ltm-per-ref-elem-graph.AC3.3 | unit | `link_score_name_dynamic_index_always_suffixed` | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_augment.rs | DynamicIndex-shape link scores always carry `:dynamic` suffix regardless of model |
| ltm-per-ref-elem-graph.AC3.3 | unit | `parse_link_offsets` tests for wildcard-suffixed scalar and A2A names | /home/bpowers/src/simlin/src/simlin-engine/src/ltm_finding.rs | Discovery parser strips `:wildcard` and `:dynamic` suffixes during `parse_link_offsets` |
| ltm-per-ref-elem-graph.AC4.1 | unit | `infer_shape_bare`, `infer_shape_fixed_single_dim`, `infer_shape_fixed_multi_dim` | /home/bpowers/src/simlin/src/simlin-engine/src/db_ltm.rs | `infer_link_shape_from_node_name` correctly classifies shape from element-level node names |
| ltm-per-ref-elem-graph.AC4.1 | integration | `a2a_loop_links_carry_bare_shape` | /home/bpowers/src/simlin/src/simlin-engine/src/db_ltm_unified_tests.rs | Pure A2A loops (`pop[r] -> births[r] -> pop[r]`) detected and all links carry `RefShape::Bare` |
| ltm-per-ref-elem-graph.AC4.2 | integration | `cross_element_fixed_index_link_carries_fixed_index_shape` | /home/bpowers/src/simlin/src/simlin-engine/src/db_ltm_unified_tests.rs | Legitimate cross-element loops via FixedIndex broadcast detected and annotated with FixedIndex shape |
| ltm-per-ref-elem-graph.AC4.2 | integration | `edge_aliasing_bare_and_fixed_index_to_same_source_element` | /home/bpowers/src/simlin/src/simlin-engine/src/db_ltm_unified_tests.rs | Edge-aliasing regression test: target with both Bare and FixedIndex(NYC) refs to same source emits both link scores; heuristic shape resolution behavior is documented and tested |
| ltm-per-ref-elem-graph.AC4.3 | integration | Full `simulate_ltm` test suite (Phase 4 Task 5 re-validation) | /home/bpowers/src/simlin/src/simlin-engine/tests/simulate_ltm.rs | All `simulate_ltm.rs` tests pass; documented per-test deltas for any tightened/loosened assertions |
| ltm-per-ref-elem-graph.AC4.3 | integration | Tightened cross_element loop-score assertions (Phase 4 Task 6) | /home/bpowers/src/simlin/src/simlin-engine/tests/simulate_ltm.rs | `test_cross_element_ltm_exhaustive` and `test_arrayed_population_ltm_exhaustive` assertions reflect actual post-fix behavior |
| ltm-per-ref-elem-graph.AC5.2 | integration | Pre-commit hook end-to-end (Phase 1 Task 7, Phase 2 Task 7, Phase 3 Task 8, Phase 4 Task 7, Phase 5 Task 6, Phase 7 Task 3) | /home/bpowers/src/simlin/scripts/pre-commit | `cargo test --workspace` completes within 3-minute wall-clock cap and all pre-commit stages pass |

## Human Verification

| AC | Justification | Verification approach |
|----|---------------|-----------------------|
| ltm-per-ref-elem-graph.AC5.1 | Pure performance/measurement criterion. Phase 5 explicitly designates this as informational and not a gate; SCC-size measurement requires running the bench binary against four fixtures and comparing pre/post numbers, which is more naturally captured as a documentation postscript than as a CI assertion. | Run `cargo run --release --example ltm_full_bench -- <fixture>` on the four fixtures (`test/cross_element_ltm`, `test/arrayed_population_ltm`, `test/hero_culture_ltm`, `test/metasd/WRLD3-03/wrld3-03.mdl`) at the pre-Phase-2 commit and at the post-Phase-4 commit; record element-level largest SCC sizes side-by-side; verify post-fix SCC <= pre-fix SCC for each fixture; append a "Measurement Postscript" section to `docs/design-plans/2026-04-25-ltm-per-ref-elem-graph.md` with the table and per-fixture commentary (Phase 5 Tasks 3, 4). |
| ltm-per-ref-elem-graph.AC6.1 | Documentation-only AC. The "Element-Level Causal Graph" section in `docs/design/ltm--loops-that-matter.md` is prose plus a per-reference table; correctness is judged by reading and confirming the narrative matches the post-Phase-2/3 implementation, not by automated assertions. | Read the rewritten section at `docs/design/ltm--loops-that-matter.md:525-548` (Phase 6 Task 1); confirm: (1) opening sentence describes per-AST-reference walking; (2) per-reference table replaces the old `ElementDependencyKind` table and matches the design plan's "Edge emission per reference site" table; (3) shape classification (Bare, FixedIndex, Wildcard, DynamicIndex) is described; (4) multidim partial-fixed conservative rule is noted; (5) structural flow->stock short-circuit is mentioned; (6) cross-references to tech-debt #20, #26, #25 and the implementation plan are present. |
| ltm-per-ref-elem-graph.AC6.2 | Documentation-only AC. The change is a single phrase update in `src/simlin-engine/CLAUDE.md` to drop the obsolete `ElementDependencyKind` reference. | Run `grep ElementDependencyKind src/simlin-engine/CLAUDE.md` (expected: no matches per Phase 6 Task 2 verification); read line 44 and confirm the replacement phrase describes per-reference walking with the new shape vocabulary (Bare, FixedIndex, Wildcard, DynamicIndex). |
| ltm-per-ref-elem-graph.AC6.3 | Documentation-only AC: tech-debt index updates require interpretation (right RESOLVED format, accurate commit SHAs, coherent measurement-reference paragraph). Automated linting cannot judge whether the prose is correct. | Read `docs/tech-debt.md` items #20 and #26 (Phase 6 Task 3); confirm Severity is `RESOLVED (date)`, the description is prefixed with the resolution note pointing to commit SHA and the design plan, and the format matches existing RESOLVED items #23 and #34. Read item #25 (Phase 6 Task 4) and confirm the appended paragraph references the Phase 5 measurement postscript and explains the threshold-retention decision. Additionally read `docs/test-plans/2026-04-04-ltm-arrays.md:58` (Phase 6 Task 5) and confirm AC8.5's spot-check sentence references `RefShape`/`collect_reference_sites`/`emit_edges_for_reference` instead of `ElementDependencyKind`. |
