# Test Requirements: Activate PREVIOUS/INIT Opcodes

This document maps each acceptance criterion from the `prev-init-opcodes` design to
either an automated test or a documented human verification step. Every sub-criterion
is accounted for exactly once.

## Automated Tests

| Criterion | Description | Test Type | Test File | Test Name |
|-----------|-------------|-----------|-----------|-----------|
| prev-init-opcodes.AC1.1 | `PREVIOUS(x)` in a scalar user model equation emits `LoadPrev` opcode (not module expansion) | Unit | `src/simlin-engine/src/db_tests.rs` | `test_previous_opcode_interpreter_vm_parity` (renamed from `test_previous_module_expansion_interpreter_vm_parity` in Phase 4 Task 1; exercises the opcode path and verifies interpreter/VM agreement) |
| prev-init-opcodes.AC1.2 | `PREVIOUS(x[DimA])` in an arrayed equation emits per-element `LoadPrev` with correct offsets | Unit | `src/simlin-engine/src/db_tests.rs` | `test_previous_of_flow_interpreter_vm_parity` (existing arrayed-context test verifies per-element opcode emission produces correct results; the A2A path through `contains_stdlib_call` and `substitute_dimension_refs` is exercised here) |
| prev-init-opcodes.AC1.3 | `PREVIOUS(x)` at the first timestep returns 0 (not x's initial value), matching module behavior | Unit | `src/simlin-engine/src/db_tests.rs` | `test_previous_returns_zero_at_first_timestep` (new test in Phase 4 Task 2; asserts `prev_x[0] == 0.0` and `prev_x[1..] == 42.0`) |
| prev-init-opcodes.AC2.1 | `INIT(x)` in a user model equation emits `LoadInitial` opcode | Unit | `src/simlin-engine/src/db_tests.rs` | `test_init_opcode_interpreter_vm_parity` (renamed from `test_init_module_expansion_interpreter_vm_parity` in Phase 4 Task 1; exercises the LoadInitial opcode path) |
| prev-init-opcodes.AC2.2 | `INIT(x)` in an aux-only model (no stocks) returns x's initial value correctly (Initials runlist includes x) | Unit | `src/simlin-engine/src/db_tests.rs` | `test_init_aux_only_model` (new test in Phase 4 Task 2; verifies `INIT(TIME*2)` freezes at 2.0 across all timesteps in a stockless model) |
| prev-init-opcodes.AC2.2 | `INIT(x)` in an aux-only model -- integration-level confirmation against reference output | Integration | `src/simlin-engine/tests/simulate.rs` | `simulates_init_builtin` (existing test; `test/builtin_init/builtin_init.stmx` is an aux-only model with `INIT(increasing)` compared against `output.tab`) |
| prev-init-opcodes.AC2.3 | `initial_values` snapshot is not all zeros when INIT references a variable with no stock dependency | Unit | `src/simlin-engine/src/db_tests.rs` | `test_init_aux_only_model` (implicitly verified: if `initial_values` were all zeros, INIT would return 0.0 instead of 2.0; the assertion `frozen == 2.0` confirms correct snapshot population) |
| prev-init-opcodes.AC3.1 | `PREVIOUS(x, init_val)` still uses module expansion and produces identical results to pre-change behavior -- scalar | Unit | `src/simlin-engine/src/db_tests.rs` | `test_2arg_previous_uses_module_expansion` (new test in Phase 4 Task 2; asserts `prev_level[0] == 99.0` confirming init_val from module expansion, not 0.0 from opcode path) |
| prev-init-opcodes.AC3.1 | `PREVIOUS(x, init_val)` still uses module expansion -- integration-level confirmation against reference output | Integration | `src/simlin-engine/tests/simulate.rs` | `simulates_previous` (existing test; `test/previous/model.stmx` uses 2-arg PREVIOUS, compared against `output.tab`) |
| prev-init-opcodes.AC3.2 | Arrayed 2-arg PREVIOUS creates per-element module instances as before | Unit | `src/simlin-engine/src/db_tests.rs` | `test_2arg_previous_uses_module_expansion` (Phase 4 Task 2 includes an arrayed variant per finalization fix #38; verifies per-element module instances produce correct dimension-indexed results) |
| prev-init-opcodes.AC4.1 | LTM link-score equations with `PREVIOUS(y)` compile to `LoadPrev` and produce correct loop scores matching reference data | Integration | `src/simlin-engine/tests/simulate_ltm.rs` | `simulates_population_ltm` (runs LTM simulation on logistic growth model, compares loop scores against `ltm_results.tsv` reference data) |
| prev-init-opcodes.AC4.1 | LTM discovery produces correct loop identification after PREVIOUS opcode switch | Integration | `src/simlin-engine/tests/simulate_ltm.rs` | `discovery_logistic_growth_finds_both_loops`, `discovery_arms_race_3party`, `discovery_decoupled_stocks`, `discovery_cross_validates_with_exhaustive` (existing LTM discovery tests validate that loop identification is unaffected by the compilation change) |
| prev-init-opcodes.AC4.2 | PREVIOUS-specific module handling removed from `compile_ltm_equation_fragment()` without affecting SMOOTH/DELAY module support | Integration | `src/simlin-engine/tests/simulate_ltm.rs` | `test_smooth_with_initial_value_ltm`, `test_smooth_goal_seeking_ltm`, `test_multiple_smooth_instances` (these tests exercise SMOOTH modules within LTM compilation; passing after the Phase 2 removal confirms SMOOTH/DELAY support is preserved) |
| prev-init-opcodes.AC5.1 | `stdlib/init.stmx` deleted, "init" removed from `MODEL_NAMES` and `stdlib_args`, no compilation errors | Unit | `src/simlin-engine/src/db_tests.rs` | `test_previous_still_in_stdlib_model_names` (renamed from `test_previous_and_init_still_in_stdlib_model_names` in Phase 3 Task 3; asserts `MODEL_NAMES.contains("previous")` and `!MODEL_NAMES.contains("init")`) |
| prev-init-opcodes.AC5.1 | Removal does not break compilation or model count | Unit | `src/simlin-engine/src/db_tests.rs` | Model count assertion updated from 9 to 8 (Phase 3 Task 3 Step 2; verifies 1 user model + 7 stdlib models after init removal) |
| prev-init-opcodes.AC6.1 | All existing integration tests pass (previous, builtin_init, LTM test models) | Integration | `src/simlin-engine/tests/simulate.rs`, `src/simlin-engine/tests/simulate_ltm.rs` | Full test suite run in Phase 5 Task 1: `simulates_previous`, `simulates_init_builtin`, all `TEST_MODELS`, all `TEST_SDEVERYWHERE_MODELS`, all LTM tests |
| prev-init-opcodes.AC6.1 | Existing PREVIOUS edge-case tests still pass | Unit | `src/simlin-engine/src/db_tests.rs` | `test_previous_of_flow_interpreter_vm_parity`, `test_previous_self_initial_value` (existing tests covering PREVIOUS of flow variables and self-referential initial values) |

## Human Verification

| Criterion | Description | Justification | Verification Approach |
|-----------|-------------|---------------|----------------------|
| prev-init-opcodes.AC4.2 | PREVIOUS-specific module handling code is actually removed from `compile_ltm_equation_fragment()` (structural, not behavioral) | Automated tests verify behavioral correctness but cannot confirm that specific dead code paths were deleted. The test suite would pass even if the dead code were left in place. | During Phase 2 code review, verify the following are removed from `db_ltm.rs`: (1) `implicit_module_vars` declaration, (2) `ltm_implicit_info` fetch, (3) PREVIOUS-in-parsed-implicit-vars block (lines 432-479), (4) cross-LTM PREVIOUS else-if branch (lines 535-605), (5) `implicit_module_vars` metadata insertion (lines 679-692). Verify the SMOOTH/DELAY path (`implicit_info.get`, `dep_variables`, `implicit_module_refs`, `implicit_submodels`) is preserved. |
| prev-init-opcodes.AC5.1 | `stdlib/init.stmx` file is actually deleted from the repository | Automated tests verify the model is not loaded, but cannot confirm the file is removed from disk/version control. A leftover file would be inert but represents incomplete cleanup. | During Phase 3 code review, verify `git rm stdlib/init.stmx` was executed and the file does not appear in `git status`. Verify the `init()` function is removed from `stdlib.gen.rs`. |
| prev-init-opcodes.AC6.2 | Scaffolding comments in bytecode.rs, codegen.rs, builtins_visitor.rs updated to reflect activated state | Comment content is not testable by automated means. Stale comments referencing "scaffolding", "deferred activation", or "Task 4 of phase 1" would be misleading but would not cause test failures. | During Phase 4 Task 3 code review, verify: (1) `builtins_visitor.rs` gate comment describes the arity-based check without scaffolding language, (2) `codegen.rs` unreachable guards at lines 931-932 and 984 reference the early-return LoadPrev/LoadInitial path without "Task 4" or "until then" language, (3) `bytecode.rs` LoadPrev/LoadInitial opcode doc comments are accurate (investigator confirmed these are already clean -- verify no regression). |
