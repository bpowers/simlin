# LTM Arrays Hardening — Phase 8 Implementation Plan

**Goal:** Remove obviated code surfaced by Phases 1 & 4, bring the LTM documentation current (`src/simlin-engine/CLAUDE.md`, `docs/design/ltm--loops-that-matter.md`, `docs/reference/ltm--loops-that-matter.md`, the measurement postscripts of the two prior LTM design plans), and close the nine GitHub issues + tick epic #488 + update `docs/tech-debt.md`.

**Architecture:** This is a cleanup/docs/tracking phase — no new functional code. Tasks: (1) a dead-code sweep (`clippy`-clean after removing the now-unused predicates / arms / nested helpers, and removing/rewriting tests pinned to old behavior); (2) doc updates (the engine CLAUDE.md module/section descriptions; the internal design doc's array-support section + a new "current limitations" entry for the residual carve-outs; a *new* array-support section in the user-facing reference doc, which currently has zero array content; re-measurement postscripts on the per-ref-elem-graph and cross-element-agg design plans); (3) GitHub: `gh issue close` the nine, edit epic #488's body to tick the boxes with commit refs; (4) `docs/tech-debt.md` updates for items #20 / #21 / #27 / #35 (there is *no* #520-related tech-debt entry — #520 is tracked only in epic #488).

**Tech Stack:** Rust (`cargo clippy`/`cargo test` for the dead-code sweep); Markdown (the docs); the `gh` CLI (for issue closure — confirmed working in this repo). The `track-issue` agent for any residual out-of-scope concerns surfaced (e.g. the non-uniform-x-spacing GF-monotonicity concern, the `LookupForward`/`LookupBackward` polarity gap).

**Scope:** Phase 8 of 8. **Depends on Phase 7** (all functional work — Phases 1-7 — complete).

**Codebase verified:** 2026-05-12 (codebase-investigator report; see "Codebase notes" below).

---

## Acceptance Criteria Coverage

### ltm-arrays-hardening.AC8: Cross-cutting — tests, golden data, docs, tracking
- **ltm-arrays-hardening.AC8.1:** `cargo test --workspace` passes within the 3-minute wall-clock cap and the pre-commit hook passes at the end of every phase. *(Phase 8: re-confirm at the end; the pre-commit hook is the gate for every commit.)*
- **ltm-arrays-hardening.AC8.2:** Phase 1's golden LTM data is byte-unchanged; every behavior-changing phase's golden-data diffs were investigated and the per-test reasoning recorded in the implementing commit message. *(Phase 8: verify the per-phase commits have the per-test reasoning; nothing new to change here unless a diff was missed.)*
- **ltm-arrays-hardening.AC8.3:** `src/simlin-engine/CLAUDE.md`, `docs/design/ltm--loops-that-matter.md`, and the user-facing `docs/reference/ltm--loops-that-matter.md` reflect the new behavior and residual limitations (the dynamic-index-reducer carve-out, RANK's treatment, the cross-agg-recovery truncation, the iterated-dimension / mapped-dimension handling).
- **ltm-arrays-hardening.AC8.4:** GH issues #520, #487, #511, #510, #514, #515, #483, #502, #492 are closed referencing the implementing commits; epic #488's checklist is ticked for each; `docs/tech-debt.md` items #21, #27, #35 and the #520-related entries are updated.

---

## Codebase notes (verified 2026-05-12)

- **Dead code after Phase 1** (these become unused once Phases 1's tasks land — confirm with `clippy`/grep at execution time):
  - `builtin_is_array_reducer` (`src/simlin-engine/src/db_analysis.rs:344`, private `fn`, doc-comment `:334-343`; one call site `:451`) — **deleted by Phase 1 Task 2**; confirm gone.
  - the inline `route_through_agg` / `routed_aggs` in `model_element_causal_edges` (`db_analysis.rs:1716-1722`) — **deleted by Phase 1 Task 4** (read from `site.routing` instead); confirm gone.
  - the nested `enumerate_shapes` fn (`db_ltm.rs:3666`) + the byte-identical `routed_aggs` filter in `emit_link_scores_for_edge` (`db_ltm.rs:4101-4104`) — **deleted by Phase 1 Task 5**; confirm gone.
  - `ltm_augment::ReducerKind` local enum (`ltm_augment.rs:1714-1731`) — **becomes a `pub(crate) use crate::ltm_agg::ReducerKind;` re-export** (Phase 1 Task 1); confirm.
  - `collect_reference_shapes` (`db_analysis.rs:252`, `pub(crate)`, re-exported `db.rs:27`) and `collect_reference_sites`/`collect_in_expr`/`classify_subscript_shape`/`resolve_literal_index` — Phase 1 moves these into `db_ltm_ir.rs` (or keeps thin shims); if `collect_reference_shapes` ends up with no callers (its callers were `enumerate_shapes` in `db_ltm.rs` and `model_edge_shapes` in `db_analysis.rs`, both of which become IR readers), **remove it and its `db.rs:27` re-export**. Confirm via `cargo clippy` (it'll flag a `pub(crate)` item with no uses if `#[deny(dead_code)]` or `-D warnings` catches it — it should).
  - `RefShape` (`db_analysis.rs:92`, re-exported `db.rs:24`) and the `RefShape::Wildcard` *variant* **survive** — they're the IR's `ClassifiedSite.shape`.
- **Dead code after Phase 4** (confirm at execution time):
  - the conservative full-cross-product arm of `emit_edges_for_reference` — at `db_analysis.rs:705` (NOT `:602` — that's the `fn` start; the `RefShape::Wildcard | RefShape::DynamicIndex` arm is at `:705`). Phase 4 either (a) narrows it to `RefShape::DynamicIndex => { ... }` only (if the IR reclassifies the `SUM(pop[idx,*])` carve-out as `DynamicIndex` — recommended) ⇒ then there is literally no `Wildcard`-cross-product arm (AC4.5 ✓ literally); or (b) keeps the combined `Wildcard | DynamicIndex` arm with a comment that `Wildcard` here only means "the dynamic-index carve-out". **Phase 8 confirms whichever Phase 4 chose and that `clippy` is clean.**
  - `link_score_var_name` (`ltm_augment.rs:855`) has NO distinct `Wildcard` arm — `Wildcard`/`DynamicIndex` already collapse into the `_ =>` catch-all (the `:wildcard`/`:dynamic` suffixes were retired in #519). Nothing to remove here; the `_ =>` arm stays (serves `FixedIndex`'s siblings + the surviving `DynamicIndex`/carve-out). The doc-comment on `link_score_var_name`/`RefShape` mentioning the retired suffixes can be tightened (the design's "the `Wildcard` case of `link_score_var_name`" is a phrasing artifact — there isn't one).
  - `expr_is_full_extent` / `reducer_is_full_reduce` (`ltm_agg.rs:643` / `:625`) — **rewritten** by Phase 4 ("compute the read slice; full-extent is the all-`Reduced` case"), not deleted. Confirm they're thin wrappers over `compute_read_slice` now.
  - `slice_reducer_subexpression_is_not_hoisted` test (`ltm_agg.rs:1287`) — **replaced** by Phase 4 with a "...is_hoisted" test asserting the right `read_slice`. `element_graph_multidim_partial_fixed_conservative` (`db_element_graph_tests.rs:817`) — **replaced** by Phase 4 with a read-slice-based test. Confirm both replacements exist.
  - `:wildcard`-suffix-era handling in `ltm_finding.rs`'s discovery parser — **NONE EXISTS** (removed in #519: `LINK_SCORE_WILDCARD_SUFFIX` / `LINK_SCORE_DYNAMIC_SUFFIX` / `strip_to_shape_suffix_with_rank` / `ShapeRank::Wildcard` are all gone; `ShapeRank` now has only `Bare`/`FixedIndex`). Nothing to remove. The *positive* guard tests (`no_wildcard_or_dynamic_link_scores_for_reducer_models` at `db_ltm_unified_tests.rs:853`, the `!name.ends_with("\u{205A}wildcard")` assertions) should be KEPT/extended.
  - `build_partial_reduce_model`'s doc comments (`tests/simulate_ltm.rs:5654-5679`) reference "Phase 4 sidesteps / Phase 5 tightens" — those refer to the *other* design plan (`2026-05-09-ltm-503-cross-element-agg.md`)'s phases, and Phase 3 (#511) of *this* plan removes the sidestep (makes `row_sum[D1]` classify `Bare`), Phase 4 (#514) tightens the `SUM(matrix[D1,*])` cross-product. Phase 8 confirms Phase 3's task removed/updated the sidestep comment; grep for `sidestep #511` / `uses bare references` (the literal phrasing varies — there's no `"sidestep #511"` literal comment today; the actual text is "an explicit `row_sum[D1]` subscript inside an apply-to-all equation classifies the reference as a dynamic index, which is a separate pre-existing limitation; Phase 4 sidesteps it" — update it to reflect that #511 fixed it).
- **Docs to update** (paths absolute under `/home/bpowers/src/simlin/`):
  - `src/simlin-engine/CLAUDE.md` (138 lines; "Last updated: 2026-05-09" at line 5 — **bump it**). LTM content is spread, not in one section: `## Incremental compilation (salsa)` has the `db_analysis.rs` bullet (line 46 — currently describes `model_element_causal_edges` walking `Expr2` ASTs via `collect_reference_sites`/`emit_edges_for_reference` and the conservative-slice/dynamic-index cross-product), the `db_ltm.rs` bullet (line 47 — describes `loop_partitions` caching `loop-id -> cycle-partition` on `LtmVariablesResult`), and the `db_ltm_*_tests`/`db_element_graph_tests` bullets (`:48-52`); `## Special features` has the `src/ltm/` bullets (`:85-91` — incl. `graph.rs` describing the `Loop` struct), `ltm_finding.rs` (`:92`), `ltm_agg.rs` (`:93` — describes the reducer set + the not-yet-hoisted conservative-slice/dynamic-index cases), `ltm_augment.rs` (`:94` — describes `classify_reducer` Linear/Nonlinear/Constant + "the obsolete per-shape `⁚wildcard`/`⁚dynamic` suffixes were retired"), `ltm_post.rs` (`:95`); `## Tests` (`:115-137`). **Phase 8 edits:** add `db_ltm_ir.rs` (the `model_ltm_reference_sites` IR — the single classification IR consumed by `model_element_causal_edges`/`model_edge_shapes`/`model_ltm_variables`) to the salsa section + the Tests section (`db_ltm_ir_tests.rs`); update the `db_analysis.rs` bullet (routing now read from the IR; no inline AST walk); update the `ltm_agg.rs` bullet (the consolidated `reducer_kind` table; `AggNode.read_slice`; sliced reducers are now hoisted; `result_dims` for synthetic aggs is the `Iterated` axes' dims; the dynamic-index reducer carve-out); update the `ltm_augment.rs` bullet (`ReducerKind` re-exported from `ltm_agg`; the iterated-dimension-subscript normalization; the analytic STDDEV partial + RANK's documented delta-ratio); update the `graph.rs`/`db_ltm.rs` bullets (element-level A2A `Loop::stocks`; `loop_partitions: HashMap<String, Vec<Option<usize>>>`; `agg_recovery_truncated` flag on `LtmVariablesResult`; cyclic-ordering cross-agg loop recovery); update the `polarity.rs`-relevant text under `src/ltm/` (per-element graphical-function link polarity; y-range-relative GF-monotonicity epsilon); note the retired Wildcard-cross-product arms.
  - `docs/design/ltm--loops-that-matter.md` (975 lines; the internal design doc). Section structure: Architecture Overview, Key Data Structures (`### CausalGraph` / `### Link` / `### Loop` / `### CyclePartitions` / `### FoundLoop`), Two Modes of Operation, Cycle Partitions, Synthetic Variable Approach, Naming Convention, Link Score Equations, Module Boundary Handling, Polarity Analysis, Strongest-Path Algorithm, **## Array Support** (`:539-821`: `### Element-Level Causal Graph` (with a `RefShape` truth table at `:557-568` that still describes the conservative-slice cross-product rows — **rewrite**), `### Aggregate Nodes`, `### Link Score Classification` (with a `classify_reducer` Linear/Nonlinear/Constant table at `:717-722`), `### Loop Scores` (mentions `MAX_AGG_PETALS` at `:793`), `### Discovery Mode`), **## Current Limitations** (`:822-837`: `### Euler Integration Only`, `### Performance on Very Large Models`), Divergences from the Papers, Test Coverage, References. **Phase 8 edits:** in `## Array Support` — describe `model_ltm_reference_sites` (the single classification IR: `ClassifiedSite` = shape + per-element key + routing; the source of truth `model_element_causal_edges`/`model_edge_shapes`/`model_ltm_variables` all read), the consolidated `reducer_kind` table, `AggNode.read_slice` and sliced-reducer hoisting (rewrite the `RefShape` truth table to drop the conservative-slice cross-product rows and add the read-slice rows), iterated-dimension-subscript handling (`row_sum[D1]` ⇒ `Bare`/same-element-on-shared-dims) and mapped-dimension references, disjoint-dim arrayed→arrayed per-source-element link scores, element-level A2A `Loop::stocks` + per-slot `loop_partitions` + per-(partition,slot) normalization, the budgeted cyclic-ordering cross-agg loop recovery + `agg_recovery_truncated`, per-element graphical-function polarity, the analytic STDDEV partial; in `## Current Limitations` — add an entry for the residual carve-outs (the dynamic-index-reducer carve-out: `SUM(pop[idx,*])` with non-literal `idx` stays unhoisted/conservative; RANK keeps the delta-ratio; the >budget cross-agg-loop truncation; multi-dim per-element-GF polarity is conservatively `Unknown`).
  - `docs/reference/ltm--loops-that-matter.md` (2066 lines; the *user-facing* paper-derived reference). Sections 1-19 including §15 "Weaknesses and Limitations" (15.1-15.7, all paper limitations). **It has ZERO array/arrayed/subscript/reducer/STDDEV/RANK content today** — §7 "Handling Discrete Elements" is about the perfect-mixing approximation, not arrays. **Phase 8 edits = net-new content:** add a new section (e.g. "## N. Arrayed Variables and Aggregate Nodes" inserted after §7 or before §15, renumbering as the doc's convention requires) describing — in the doc's user-facing register — how LTM treats subscripted variables (element-level loops, the element-level causal graph, treating an inlined array reducer as an implicit aggregate node trimmed from reported loops, per-element vs cross-element loops, the iterated-dimension/mapped-dimension handling, partial-reduce hoisting); and add entries under §15 for the residual limitations users would hit (the dynamic-index-reducer carve-out, RANK's treatment, the >budget cross-agg-loop truncation — phrased as "if your model has a reducer in a feedback loop over a very large dimension, the cross-element loop list may be truncated; a warning is emitted"). **Also update `docs/README.md`** if any section/file is added or renamed (per `docs/CLAUDE.md` — but Phase 8 mostly *edits*; if you only add a section *within* an existing doc, no README change is needed; the README already lists all the LTM docs).
  - `docs/design-plans/2026-04-25-ltm-per-ref-elem-graph.md` — has a `## Measurement Postscript` section (`:573`) with two blocks (the 2026-04-25 original + `### Re-measurement after the cross-element aggregate-scoring work (2026-05-09)` at `:640`). **Phase 8 adds a third subsection** `### Re-measurement after the arrays-hardening work (2026-05-…)` re-running the `measure_tiered(path)` / `measurement_postscript_*` tests (`tests/simulate_ltm.rs:~4220-4290`) on reducer-bearing fixtures (`share`/`mean`/the cross-element fixtures + any sliced-reducer fixture) and recording the var/elem SCC sizes, var/elem circuit counts, fast-path/slow-path counts before vs after.
  - `docs/design-plans/2026-05-09-ltm-503-cross-element-agg.md` — exists; **Phase 8 adds a note** (a paragraph, e.g. in a new "## Postscript" section or appended to `## Out of Scope`) pointing at the re-measurement in the per-ref-elem-graph postscript and noting which of its "future work" items the arrays-hardening cluster resolved (the conservative-slice carve-out via #514; etc.).
  - `docs/tech-debt.md` — `## Items` with numbered `### N.` entries. **Item #20** ("LTM FixedIndex References Expand to N-squared Edges", RESOLVED 2026-04-25, lines `~181-188`) — its trailing clause "(the conservative-slice case `SUM(pop[NYC,*])` and bare dynamic indices stay full-cross-product -- GH #514)" — **update**: #514 (Phase 4) resolved the conservative-slice case (now hoisted via `AggNode.read_slice`); only the *bare dynamic index* (`SUM(pop[idx,*])`, `arr[i+1]`) stays full-cross-product (a deliberate narrow carve-out). **Item #21** ("LTM Polarity Analysis Has Reducer Blind Spots", medium, `Tracked in: #480`, lines `~190-198`) — the "plateau-tolerant GF monotonicity check" half is Phase 7's #492; mark that half resolved (the y-range-relative epsilon); the reducer-cases half was largely #480 (closed) — note the GF half is done in #492. **Item #27** ("LTM STDDEV/RANK Fallback Scores Are Silently Wrong", medium, `Tracked in: #483`, lines `~244-252`, has a `Note (2026-04-29)` citing `ltm_augment.rs:1374-1381` which is stale — actual is `:2155-2162`) — mark STDDEV RESOLVED (Phase 6's analytic partial); document RANK's treatment (kept on the delta-ratio with a justification, pinned by `test_generate_rank_keeps_delta_ratio`). **Item #35** ("A2A Loops Get `partition = None` in `loop_partitions`", medium, `Tracked in: #487`, lines `~314-325`) — mark RESOLVED (Phase 2's element-level `Loop::stocks` + per-slot `loop_partitions` + per-(partition,slot) normalization). **There is NO `#520`-related entry in `docs/tech-debt.md`** (grep for "520"/"walker"/"classification IR"/"reference-site" finds nothing — #520 is tracked only in epic #488's "Core algorithm" checklist) — so the design's "update the #520-related entries" is satisfied by "there's nothing to update"; optionally add a RESOLVED breadcrumb entry if it adds value, but it's not required.
- **GitHub** — `gh` CLI works in this repo (`gh issue list -R bpowers/simlin --limit 5` returns results). All nine target issues exist and are OPEN: #520 ("ltm: unify the element-graph and link-score reference-site walkers behind a single classification IR", labels `ltm`), #487 ("ltm: rel_loop_score normalisation cross-pollutes independent A2A loops", `engine`+`ltm`), #511 ("ltm-augment: subscripted-A2A-reference link-score partial fails to compile (PREVIOUS arg must be Var)", `ltm`), #510 ("ltm: degenerate link score for disjoint-dimension arrayed-to-arrayed edges with per-element target equations", `ltm`), #514 ("ltm: sliced reducer sub-expressions are not hoisted into aggregate nodes", `ltm`), #515 ("ltm: MAX_AGG_PETALS=8 caps (and drops) cross-element-through-aggregate loop recovery; per-subset enumeration", `ltm`), #483 ("ltm-augment: STDDEV/RANK partials silently fall back to delta-ratio", `engine`+`ltm`), #502 ("ltm: support per-element graphical functions in static link polarity analysis", `ltm`), #492 ("ltm: GF strict-monotonicity check (EPSILON=1e-10) flags numeric-import noise as Unknown", `engine`+`ltm`). **#488 is an epic** ("epic: Loops that Matter (LTM) -- tracking known issues and improvements", labels `epic`+`engine`+`ltm`) with GitHub task-lists grouped by area: "Core algorithm — `ltm.rs`, `db_analysis.rs`" (contains `- [ ] #514`, `- [ ] #492`, `- [ ] #502`, `- [ ] #520`), "Discovery / post-sim — `ltm_finding.rs`, `ltm_post.rs`" (contains `- [ ] #487`, `- [ ] #515`), "Augmentation — `ltm_augment.rs`" (contains `- [ ] #483`, `- [ ] #510`, `- [ ] #511`), plus "Module / array umbrellas + FFI" and "Documentation"; it already converts resolved items to `- [x] #503 -- ... (done in <commit range>)`. **Phase 8:** `gh issue close N -R bpowers/simlin --comment "Resolved by <commit-sha(s)> (Phase <P> of the LTM arrays hardening plan, docs/design-plans/2026-05-11-ltm-arrays-hardening.md)."` for each of the nine; then `gh issue edit 488 -R bpowers/simlin --body <updated>` (or via the API) changing the nine `- [ ] #N` to `- [x] #N -- <one-line> (done in <commit-sha>)`. (Don't close anything until all the implementing commits exist and the per-phase code reviews passed.)
- **No standard "## Postscript" / "## Implementation Notes" convention** for design plans generally — the `## Measurement Postscript` is specific to the measurement-heavy LTM plans (the per-ref-elem-graph one and the variable-level-loop-enumeration one). `2026-05-11-ltm-arrays-hardening.md` itself has no postscript section and doesn't require one — Phase 8's docs work targets the *other* docs listed above, not a postscript on the hardening plan (though adding one summarizing the actual outcomes is a reasonable non-conventional extra if it helps future readers).
- **Subcrate CLAUDE.md** — only `src/libsimlin/CLAUDE.md` has any LTM/analysis content (its `### Analysis` section `:49-53` lists `simlin_analyze_get_loops()` etc.; it does NOT name `loop_partitions`/`Loop::stocks`). If Phase 2 added a new C accessor (it shouldn't have — the per-slot rel-loop-score is reachable via the existing subscripted-loop-id accessor), add a bullet there. `src/engine/CLAUDE.md`, `src/pysimlin/CLAUDE.md`, `src/diagram/CLAUDE.md`, `src/core/CLAUDE.md` have no LTM mention — nothing to update.
- **Testing conventions:** `cargo test --workspace` 3-min cap, no `--no-verify`; the pre-commit hook (`scripts/pre-commit`) runs the Rust pipeline (`cargo fmt --check` → cbindgen header diff → `cargo clippy --all-targets --all-features -- -D warnings` ∥ `timeout 180 cargo test`) + the TS pipeline + the Python pipeline + `python3 scripts/check-deps.py` + `python3 scripts/check-docs.py` + `bash scripts/lint-project.sh` (the doc-link check `check-docs.py` will catch a broken link if a `docs/README.md` entry doesn't match a renamed file — so if Phase 8 renames a doc, update README.md in the same commit).

---

## Tasks

<!-- START_TASK_1 -->
### Task 1: Dead-code sweep — remove obviated predicates / arms / nested helpers; rewrite tests pinned to old behavior

**Verifies:** ltm-arrays-hardening.AC8.1 (clippy-clean `cargo test --workspace`), and consolidates the "remove obviated code" / "remove or rewrite tests pinned to old behavior" deliverables.

**Files:** (confirm each is actually dead with `cargo clippy --workspace --all-targets --all-features -- -D warnings` and targeted grep before removing — most should already have been removed by the relevant Phase's tasks; this task is the catch-all sweep + verification)
- `src/simlin-engine/src/db_analysis.rs` — confirm `builtin_is_array_reducer`, the inline `route_through_agg`/`routed_aggs` in `model_element_causal_edges`, and (if moved by Phase 1) `collect_reference_sites`/`collect_in_expr`/`classify_subscript_shape`/`resolve_literal_index`/`collect_reference_shapes` are gone or relocated; confirm `RefShape`/`emit_edges_for_reference`/`expand_same_element`/`cartesian_element_names` remain (the conservative arm at `:705` is narrowed to `DynamicIndex`-only if Phase 4 reclassified the carve-out, else keeps the comment).
- `src/simlin-engine/src/db_ltm.rs` — confirm the nested `enumerate_shapes` fn and the `routed_aggs` filter in `emit_link_scores_for_edge` are gone; confirm `MAX_AGG_PETALS` was repurposed/removed and the new loop-count budget exists.
- `src/simlin-engine/src/db.rs` — confirm the `collect_reference_shapes` re-export (`:27`) is removed if `collect_reference_shapes` was deleted.
- `src/simlin-engine/src/ltm_augment.rs` — confirm `ReducerKind` is a re-export of `ltm_agg::ReducerKind`; confirm `is_array_reducer_name`/`classify_reducer`/`agg_reducer_is_monotone`-relevant code reads `reducer_kind`; tighten the `link_score_var_name`/`RefShape` doc-comments mentioning the retired `⁚wildcard`/`⁚dynamic` suffixes.
- `src/simlin-engine/src/ltm_agg.rs` — confirm `expr_is_full_extent`/`reducer_is_full_reduce` are thin wrappers over `compute_read_slice`; confirm `AggNode.read_slice`/`AxisRead` exist; confirm `reducer_kind`/`reducer_kind_from_name`/the monotone predicate are the single recognition table.
- Tests: confirm `slice_reducer_subexpression_is_not_hoisted` (`ltm_agg.rs:1287`) is replaced by a "...is_hoisted" test with the right `read_slice`; confirm `element_graph_multidim_partial_fixed_conservative` (`db_element_graph_tests.rs:817`) is replaced by a read-slice test; confirm the `db_ltm_unified_tests.rs` positive guard tests (`no_wildcard_or_dynamic_link_scores_for_reducer_models`, the `!name.ends_with("\u{205A}wildcard")` assertions) are kept; confirm `build_partial_reduce_model`'s "Phase 4 sidesteps #511" doc text (`tests/simulate_ltm.rs:5654-5679`) was updated by Phase 3 to reflect that #511 fixed it (and Phase 4 tightened the cross-product). Grep `src/simlin-engine/` for any remaining reference to the removed symbols (`builtin_is_array_reducer`, `enumerate_shapes`, `collect_reference_shapes` if removed, `MAX_AGG_PETALS` if renamed) — fix or remove.

**Verification:** `cargo clippy --workspace --all-targets --all-features -- -D warnings` — **zero warnings** (no dead code, no unused imports). `cargo test --workspace` — green within the 3-minute cap (`time cargo test --workspace` to confirm). If anything is still flagged dead, remove it; if a test still pins old behavior, rewrite it (don't `#[allow(dead_code)]` it away). Commit and run the pre-commit hook; no `--no-verify`.

**Commit:** `engine: dead-code sweep after LTM arrays hardening` (or fold into the relevant phase's commit if no straggler is found — most should already be clean).

<!-- END_TASK_1 -->

<!-- START_TASK_2 -->
### Task 2: Documentation — `src/simlin-engine/CLAUDE.md`, `docs/design/ltm--loops-that-matter.md`, `docs/reference/ltm--loops-that-matter.md`, the measurement postscripts

**Verifies:** ltm-arrays-hardening.AC8.3 (the engine CLAUDE.md, the internal design doc, and the user-facing reference reflect the new behavior + residual limitations).

**Files:**
- Modify: `src/simlin-engine/CLAUDE.md` — bump "Last updated:" (line 5); update the `db_analysis.rs` / `db_ltm.rs` / `ltm_agg.rs` / `ltm_augment.rs` / `src/ltm/` bullets and add a `db_ltm_ir.rs` bullet (in the salsa section) + a `db_ltm_ir_tests.rs` line (in the Tests section), per "Codebase notes / Docs to update" above. Keep it concise — CLAUDE.md is a map, not a treatise; one updated/added clause per item.
- Modify: `docs/design/ltm--loops-that-matter.md` — in `## Array Support`: rewrite the `RefShape` truth table to drop the conservative-slice cross-product rows and add read-slice rows; describe `model_ltm_reference_sites` (the classification IR), the consolidated `reducer_kind` table, `AggNode.read_slice` + sliced-reducer hoisting, iterated-dimension-subscript handling (⇒ `Bare`/same-element-on-shared-dims) + mapped-dimension references, disjoint-dim arrayed→arrayed per-source-element link scores, element-level A2A `Loop::stocks` + per-slot `loop_partitions` + per-(partition,slot) normalization, the budgeted cyclic-ordering cross-agg loop recovery + `agg_recovery_truncated`, per-element graphical-function polarity, the analytic STDDEV partial; in `## Current Limitations`: add an entry for the residual carve-outs (dynamic-index reducer; RANK delta-ratio; >budget cross-agg-loop truncation; multi-dim per-element-GF polarity conservatively `Unknown`). Update the `### Loop Scores` `MAX_AGG_PETALS` mention to the new budget. Update `## Test Coverage` and `## Divergences from the Papers` if the array material there is now stale.
- Modify: `docs/reference/ltm--loops-that-matter.md` — add a new section "## N. Arrayed Variables and Aggregate Nodes" (insert after §7 "Handling Discrete Elements" or before §15, renumbering subsequent sections and updating the table of contents / cross-references — follow the doc's existing numbering convention; check whether sections are numbered in the heading text or auto-numbered): describe, in the user-facing register, element-level loops, the element-level causal graph, treating an inlined array reducer (whole-extent or sliced) as an implicit aggregate node trimmed from reported loops, per-element vs cross-element loops, the iterated-dimension / mapped-dimension handling, partial-reduce hoisting; add entries under §15 "Weaknesses and Limitations" for the residual limitations a user would encounter (the dynamic-index-reducer carve-out — "a reducer indexed by a dynamic/computed index isn't hoisted, so its contribution shows up as a coarse conservative edge"; RANK's treatment — "RANK link scores use a delta-ratio approximation"; the >budget cross-agg-loop truncation — "for a reducer in a feedback loop over a very large dimension, the recovered cross-element loop list may be incomplete; a warning is emitted").
- Modify: `docs/design-plans/2026-04-25-ltm-per-ref-elem-graph.md` — add `### Re-measurement after the arrays-hardening work (2026-05-…)` to the `## Measurement Postscript` section, with a table mirroring the `2026-05-09` block's columns (var SCC / elem SCC / var circuits / elem circuits / fast-path / slow-path / slow-path SCC) on the reducer-bearing fixtures (`share`, `mean`, the cross-element fixtures, plus a sliced-reducer fixture if one was added) — run `measure_tiered(path)` / the `measurement_postscript_*` tests in `tests/simulate_ltm.rs` (`~:4220-4290`) before-vs-after (the "before" is the `2026-05-09` block; the "after" is post-Phases-1-7).
- Modify: `docs/design-plans/2026-05-09-ltm-503-cross-element-agg.md` — append a short note (a new "## Postscript" section, or a paragraph in `## Out of Scope`) pointing at the re-measurement in the per-ref-elem-graph postscript and noting that the arrays-hardening cluster (this plan) resolved the conservative-slice carve-out (#514), the rel-loop-score cross-pollution (#487), the iterated-dimension limitation (#511), and the disjoint-dim degenerate-link-score (#510).
- Modify: `docs/README.md` — only if a doc was renamed or a new file added; if Phase 8 only adds *sections within* existing docs, no change. (The `scripts/check-docs.py` hook will catch a stale link.)

**Verification:** `python3 scripts/check-docs.py` (the doc-link check — also run by the pre-commit hook) — passes. Re-read each edited doc for consistency (no contradiction between `docs/design/...` and `docs/reference/...`; the residual-limitations lists match). Commit and run the pre-commit hook; no `--no-verify`.

**Commit:** `doc: update LTM docs for the arrays-hardening cluster`

<!-- END_TASK_2 -->

<!-- START_TASK_3 -->
### Task 3: GitHub issue closure; tick epic #488; `docs/tech-debt.md` updates; track residual concerns

**Verifies:** ltm-arrays-hardening.AC8.4 (the nine issues closed referencing the implementing commits; epic #488 ticked; tech-debt items #21/#27/#35 updated — and the #520-related entries, of which there are none in `docs/tech-debt.md`, confirmed).

**Files:**
- Modify: `docs/tech-debt.md` — **item #20**: update the trailing clause from "(the conservative-slice case `SUM(pop[NYC,*])` and bare dynamic indices stay full-cross-product -- GH #514)" to note #514 resolved the conservative-slice case (hoisted via `AggNode.read_slice`) and only the *bare dynamic index* (`SUM(pop[idx,*])`, `arr[i+1]`) stays full-cross-product (a deliberate carve-out); add a `Note (2026-05-…)` line. **Item #21**: mark the GF-monotonicity half resolved (the y-range-relative epsilon, #492); add a `Note`. **Item #27**: mark STDDEV RESOLVED (#483's analytic partial); document RANK's treatment (delta-ratio with a justification, pinned by `test_generate_rank_keeps_delta_ratio`); fix the stale `ltm_augment.rs:1374-1381` line ref to `:2155-2162` (or just say "in `generate_nonlinear_partial`"); add a `Note`. **Item #35**: mark RESOLVED (#487's element-level `Loop::stocks` + per-slot `loop_partitions` + per-(partition,slot) normalization); add a `Note`. Confirm there's no `#520`-related entry to update (grep) — if you want, add a brief RESOLVED breadcrumb entry "### N. LTM had three parallel reference-site AST walkers — RESOLVED (2026-05-…): unified behind `model_ltm_reference_sites` (#520)", but it's optional. Update each touched item's `Last reviewed:` date.
- GitHub (via `gh`):
  - For each of #520, #487, #511, #510, #514, #515, #483, #502, #492: `gh issue close N -R bpowers/simlin --comment "Resolved by <implementing-commit-sha(s)> — Phase <P> of the LTM arrays hardening plan (docs/design-plans/2026-05-11-ltm-arrays-hardening.md)."` (fill in the actual commit SHAs from the per-phase commits; one issue may span >1 commit — list them all).
  - Edit epic #488's body (`gh issue view 488 -R bpowers/simlin --json body -q .body` to fetch it, edit, `gh issue edit 488 -R bpowers/simlin --body-file <tmp>`): change `- [ ] #520`, `- [ ] #514`, `- [ ] #492`, `- [ ] #502` (in "Core algorithm"), `- [ ] #487`, `- [ ] #515` (in "Discovery / post-sim"), `- [ ] #483`, `- [ ] #510`, `- [ ] #511` (in "Augmentation") to `- [x] #N -- <one-line> (done in <commit-sha>)`, matching the existing `- [x] #503 -- ... (done in ...)` format. Don't touch the other unchecked items.
- Spawn the `track-issue` agent (via the Task tool, `subagent_type: "track-issue"`) for the residual out-of-scope concerns the design / Phase 7 surfaced: (a) the non-uniform-x-spacing GF-monotonicity concern (`dy` vs slope `dy/dx` in `analyze_graphical_function_polarity` — out of #492's scope, "noted for separate tracking"); (b) the `LookupForward`/`LookupBackward` static-link-polarity gap (only `BuiltinFn::Lookup` is handled by `analyze_expr_polarity_with_context` — if Phase 7 didn't extend the new `Subscript` arm to all three `Lookup*` variants); (c) anything else flagged during execution (e.g. if the AC5.2 `(m-1)!/2` ambiguity was resolved one way, the other reading might be worth a follow-up note). One `track-issue` invocation per concern (or one with a list — the agent dedups against GitHub issues and `docs/tech-debt.md`).

**Verification:** `git diff -- docs/tech-debt.md` reviewed. `gh issue list -R bpowers/simlin --state closed --search "label:ltm" --limit 15` shows the nine closed; `gh issue view 488 -R bpowers/simlin` shows the nine boxes ticked with commit refs. The `track-issue` agent reports either "filed" or "already tracked" for each residual concern. Commit the `docs/tech-debt.md` change and run the pre-commit hook; no `--no-verify`.

**Commit:** `doc: tech-debt updates; close LTM arrays hardening issues` (the `gh` operations are not commits; the only file change is `docs/tech-debt.md` — possibly fold into Task 2's `doc:` commit).

<!-- END_TASK_3 -->

<!-- START_TASK_4 -->
### Task 4: Final whole-workspace verification

**Verifies:** ltm-arrays-hardening.AC8.1 (the whole `cargo test --workspace` passes within the 3-minute cap; the pre-commit hook passes), and a final sanity pass over AC8.2 (every behavior-changing phase's golden diffs were investigated + documented in its commit).

**Files:** none (verification only).

**Verification steps:**
1. `time cargo test --workspace` — green, under 3 minutes wall-clock. If it regressed, profile (`cargo test --workspace 2>&1 | grep 'finished in'`) and address (nothing in this cluster should add meaningful runtime; the IR's once-per-variable walk is cheaper than the prior per-edge re-walks).
2. `cargo test -p simlin-engine --features file_io --test simulate_ltm` and `--test simulate` and `--test wrld3_ltm_panic` — green.
3. `pnpm build && pnpm tsc && pnpm test` (the TS pipeline — also run by the pre-commit hook) — green. `scripts/pysimlin-tests.sh` (the Python pipeline) — green.
4. `cargo clippy --workspace --all-targets --all-features -- -D warnings` — zero warnings.
5. `git log --oneline main..HEAD` — confirm each behavior-changing phase's commit message records the per-test golden-diff reasoning (AC8.2); if any commit is missing it, that's a process gap to note (you can't rewrite history mid-merge, but flag it — and the design's discipline was "investigate every diff", so a missing rationale means re-examining whether the diff was actually correct).
6. `git diff main..HEAD -- test/` — `logistic_growth_ltm/ltm_results.tsv` byte-unchanged (the only checked-in LTM golden file; Phase 1 must not have touched it, later phases shouldn't either since `logistic_growth` is scalar); any other `test/` change is unexpected — investigate.
7. Run the pre-commit hook one final time (commit nothing, or commit a trivial no-op if there's a doc straggler) — confirm `cargo fmt --check`, the cbindgen header diff, `cargo clippy`, `timeout 180 cargo test`, the TS pipeline, the Python pipeline, `check-deps.py`, `check-docs.py`, `lint-project.sh` all pass.

**Commit:** none (or fold a final doc touch-up into Task 2/3).

<!-- END_TASK_4 -->

---

## Phase 8 done when

- No dead code surfaced by Phases 1 & 4 remains (`cargo clippy --workspace --all-targets --all-features -- -D warnings` is clean); tests pinned to old behavior (`slice_reducer_subexpression_is_not_hoisted`, `element_graph_multidim_partial_fixed_conservative`, the `build_partial_reduce_model` "sidestep #511" doc text) are rewritten/updated.
- `src/simlin-engine/CLAUDE.md`, `docs/design/ltm--loops-that-matter.md`, and the user-facing `docs/reference/ltm--loops-that-matter.md` reflect the new behavior (the `model_ltm_reference_sites` IR, the consolidated `reducer_kind` table, `AggNode.read_slice` + sliced-reducer hoisting, iterated-dimension / mapped-dimension handling, disjoint-dim arrayed→arrayed link scores, element-level A2A `Loop::stocks` + per-slot `loop_partitions`, the budgeted cyclic-ordering cross-agg loop recovery + `agg_recovery_truncated`, per-element graphical-function polarity, the analytic STDDEV partial) and the residual limitations (the dynamic-index-reducer carve-out, RANK's treatment, the cross-agg-recovery truncation, the iterated-dimension / mapped-dimension handling, multi-dim per-element-GF polarity conservatively `Unknown`) (AC8.3); the per-ref-elem-graph design plan has a re-measurement postscript and the cross-element-agg design plan has a note about it.
- GH issues #520, #487, #511, #510, #514, #515, #483, #502, #492 are closed referencing the implementing commits; epic #488's checklist is ticked for each with commit refs; `docs/tech-debt.md` items #20, #21, #27, #35 are updated (and the absence of a #520-related entry is confirmed); the residual out-of-scope concerns (non-uniform-x-spacing GF monotonicity; the `LookupForward`/`LookupBackward` polarity gap if not closed in Phase 7) are filed via the `track-issue` agent (AC8.4).
- `cargo test --workspace` passes within the 3-minute cap; the pre-commit hook (Rust + TS + Python + the doc/dep/lint checks) passes (AC8.1); Phase 1's golden LTM data is byte-unchanged and every behavior-changing phase's golden diffs were investigated and documented in its commit (AC8.2).
