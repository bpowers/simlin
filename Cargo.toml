[workspace]

members = [
    "src/engine-v2",
    "src/mdl",
    "src/system-dynamics-compat",
    "src/system-dynamics-engine",
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
lto = true
debug = 1
