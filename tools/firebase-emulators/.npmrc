; This install is deliberately standalone: keep pnpm from treating it as
; part of the repo workspace so the ~600-package firebase-tools tree never
; enters the main lockfile or a normal `pnpm install`.
ignore-workspace=true
